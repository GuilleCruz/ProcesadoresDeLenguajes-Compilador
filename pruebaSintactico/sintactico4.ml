%----------------------------------
programa sintactico4; 
%----------------------------------
%% Instrucción de selección incorrecta (fno) %%
%% Expresión incorrecta %%

	entero a, b, c, d;
	booleano adios, hola;
	caracter y, f;

%----------------------------------
accion fallo (ref entero m; ref caracter u);
%----------------------------------
principio
	si 2 = 2 ent
		escribir("si");
	si_no
		escribir("no");
	fno
fin

principio
	b := (2-2)*5*8*)34(+2;
fin